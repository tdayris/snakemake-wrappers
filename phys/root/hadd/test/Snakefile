rule hadd:
    input:
        expand("ntuple{n}.root", n=range(3)),
    output:
        "ntuple.root",
    log:
        "logs/hadd/hadd.log",
    threads: 2
    params:
        extra="-f5",
    wrapper:
        "master/phys/root/hadd"
