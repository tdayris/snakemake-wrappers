rule wisecondor_convert_trio:
	input:
		bam = "{basedir}/{analysis}/{prefix}/{prefix}_wisecondor_merged.bam",
		bai = "{basedir}/{analysis}/{prefix}/{prefix}_wisecondor_merged.bam.bai"
	output:
		npz = "{basedir}/{analysis}/{prefix}/{prefix}_wisecondor_merged.npz"
	log:
		"{basedir}/{analysis}/log/{prefix}/{prefix}_wisecondor_merged.log"
	resources: io=10
	shell:
		"WisecondorX convert {input.bam} {output.npz} 2>&1 | tee -a {log}"
