rule rsync_vcf_tmb:
	input:
		"{basedir}/{analysis}/{prefix}/sign_mut/{prefix}_concat.vcf"
	output:
		"{basedir}/{analysis}/{prefix}/{prefix}_concat.vcf"
	log:
		"{basedir}/{analysis}/log/{prefix}/rsync_vcf.log"
	shell:
		"rsync -Paz {input} {output} 2>&1 | tee -a {log}"
