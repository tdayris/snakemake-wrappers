rule sambamba_index_wgs:
	input:
		"{basedir}/{analysis}/{sample}/{sample}_sorted.bam"
	output:
		"{basedir}/{analysis}/{sample}/{sample}_sorted.bam.bai"
	resources: io=3
	log:
		"{basedir}/{analysis}/log/{sample}/{sample}_sambamba_index.log"
	params:
		sambamba_index_options = config["sambamba_index"]["OPTIONS"]
	shell:
		"sambamba index {params.sambamba_index_options} {input} {output} 2>&1 | tee -a {log}"
