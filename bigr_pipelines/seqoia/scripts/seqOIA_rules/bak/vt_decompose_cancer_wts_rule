rule vt_decompose_cancer_wts:
	input:
		"{basedir}/{analysis}/{prefix}/chr_{index}/{prefix}_chr_{index}_wts.vcf"
	output:
		temp("{basedir}/{analysis}/{prefix}/chr_{index}/{prefix}_chr_{index}_wts_VTD.vcf")
	log:
		"{basedir}/{analysis}/{prefix}/chr_{index}/{prefix}_chr_{index}_wts_VTD.log"
	params:
		vt_decompose_options=config["vt_decompose"]["OPTIONS"]
	shell:
		"docker_wrapper -c 'vt decompose {params.vt_decompose_options} {input} -o {output}' 2>&1 | tee -a {log}"
