rule sambamba_index_wts:
	input:
		"{basedir}/{analysis}/{sample}/{sample}_star_sorted.bam"
	output:
		"{basedir}/{analysis}/{sample}/{sample}_star_sorted.bam.bai"
	resources: io=3
	log:
		"{basedir}/{analysis}/log/{sample}/{sample}_star_sambamba_index.log"
	params:
		sambamba_index_options = config["sambamba_index"]["OPTIONS"]
	shell:
		"sambamba index {params.sambamba_index_options} {input} {output} 2>&1 | tee -a {log}"
