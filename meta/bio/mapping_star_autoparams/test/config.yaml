pathvars:
  species: "{species}"
  build: "{build}"
  release: "{release}"
  upstream_reads: "tmp/make_reads_available/{sample}.1.fastq.gz"
  downstream_reads: "tmp/make_reads_available/{sample}.2.fastq.gz"
  genome_sequence: "ref/genome.chr21.fa"
  genome_index: "ref/genome.chr21.fa.fai"
  genome_annotation: "ref/annotation.chr21.gtf"
  genome_agat_statistics: "statistics.yaml"
samples:
  - a
  - b
