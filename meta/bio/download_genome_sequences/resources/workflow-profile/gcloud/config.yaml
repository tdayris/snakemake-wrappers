set-resources:
  reference_ensembl_sequence_download:
    googlebatch_memory: min(attempt * 400, 1500)
    googlebatch_max_run_duration: str(min(attempt * 30 * 60, 240 * 60)) + "s"
    googlebatch_boot_disk_gb: max(30, 6 * attempt)
    googlebatch_machine_type: "e2-micro"

  pyfaidx_filter:
    googlebatch_memory: min(attempt * 400, 1500)
    googlebatch_max_run_duration: str(min(attempt * 30 * 60, 240 * 60)) + "s"
    googlebatch_boot_disk_gb: max(30, 6 * attempt)
    googlebatch_machine_type: "e2-micro"

  samtools_faidx:
    googlebatch_memory: min(attempt * 400, 1500)
    googlebatch_max_run_duration: str(min(attempt * 30 * 60, 240 * 60)) + "s"
    googlebatch_boot_disk_gb: max(30, 6 * attempt)
    googlebatch_machine_type: "e2-micro"

  picard_create_sequence_dictionary:
    googlebatch_memory: min(attempt * 1500, 8000)
    mem_mb: min(attempt * 1500, 8000)
    googlebatch_max_run_duration: str(min(attempt * 30 * 60, 240 * 60)) + "s"
    googlebatch_boot_disk_gb: max(30, 6 * attempt)
    googlebatch_machine_type: "e2-micro"

  fasta_to_twobit_convert:
    googlebatch_memory: min(attempt * 400, 1500)
    googlebatch_max_run_duration: str(min(attempt * 30 * 60, 240 * 60)) + "s"
    googlebatch_boot_disk_gb: max(30, 6 * attempt)
    googlebatch_machine_type: "e2-micro"

  xan_extract_chrom_sizes:
    googlebatch_memory: min(attempt * 400, 1500)
    googlebatch_max_run_duration: str(min(attempt * 30 * 60, 240 * 60)) + "s"
    googlebatch_boot_disk_gb: max(30, 6 * attempt)
    googlebatch_machine_type: "e2-micro"
