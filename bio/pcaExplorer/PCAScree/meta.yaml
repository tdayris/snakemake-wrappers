---
name: pcaExplorer PCA Scree
description: Plot PCA loadings from a vst/rld transformed dds object
authors:
  - Thibault Dayris
input:
  - A normalized dds (vst/rlog) of all samples and targets
output:
  - A png image of the PCA loadings
notes: |
  Parameters:

  * extra (str) Command line arguments (interpreted by R)
