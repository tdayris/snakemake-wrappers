rule samtools_markdup:
    input:
        aln="{sample}.bam",
    output:
        bam="{sample}.markdup.bam",
        idx="{sample}.markdup.bam.csi",
    log:
        "{sample}.markdup.log",
    threads: 2
    params:
        extra="-c --no-PG",
    wrapper:
        "master/bio/samtools/markdup"
