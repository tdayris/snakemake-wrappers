rule test_SomaticSignatures_plotMutationSpectrum:
    input:
        context = "context.rds"
    output:
        png = "plot.png"
    params:
        extra = "group = 'sampleNames'"
    wrapper:
        "master/bio/SomaticSignatures/plotMutationSpectrum"
