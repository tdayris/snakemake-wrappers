rule test_mutationContext:
    input:
        call = "calls.rds",
        sequence = "sequence.rds"
    output:
        rds = "context.rds"
    wrapper:
        "master/bio/SomaticSignatures/mutationContext"
