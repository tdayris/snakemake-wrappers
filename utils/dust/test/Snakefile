rule test_dust:
    input:
        # Optional path to a directory (defaults to '.')
        path="../",
        # Optional path to a configuration file 
        config="config.toml",
    output:
        "disk_usage.json",
    log:
        "test_dust.log",
    threads: 1
    params:
        extra="--depth 2",
    wrapper:
        "master/utils/dust"
